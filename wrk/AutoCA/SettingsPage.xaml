<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AutoCA.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AutoCA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Grid Grid.Column="0" Grid.Row="0" Style="{StaticResource CaptionPane}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource CaptionText}">設定</TextBlock>
			<TextBlock Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource CaptionText}"></TextBlock>
		</Grid>
		<StackPanel Grid.Column="0" Grid.Row="1" Orientation="Vertical" Style="{StaticResource CardStyleStackPanel}">
			<TextBox Margin="8,8,8,8" Header="共通名" PlaceholderText="" ></TextBox>
			<TextBox Margin="8,8,8,8" Header="団体名" PlaceholderText="" ></TextBox>
			<TextBox Margin="8,8,8,8" Header="部署名" PlaceholderText="" ></TextBox>

			<Grid Margin="8,8,8,8">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<TextBox Grid.Column="0" Grid.Row="0" Header="組織名" PlaceholderText="" ></TextBox>
			</Grid>
		</StackPanel>
		<!-- 項目の追加は、下の方が簡単だが、上の方が見栄えはよい。 -->
		<ListView Grid.Column="0" Grid.Row="2" ItemsSource="{x:Bind pParameters}">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="local:Parameter">
					<Grid Margin="4,4,4,4">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>

						<TextBox Grid.Column="0" Grid.Row="0" Header="{x:Bind ItemName}" PlaceholderText="" Text="{x:Bind ItemValue}" />
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</Page>