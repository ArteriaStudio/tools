<Page
	x:Class="StatusBook.ListupPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:StatusBook"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- 幅に関する留意事項：自動（auto/default）にした場合、配置する要素を自動改行せず、全てを並べられる幅まで拡げる挙動を取る。（ウィンドウサイズから動的に寸法を指定する必要があると思われる：2022/08/21 Rink） -->
		<GridView ItemsSource="{x:Bind pItems}">
			<GridView.ItemTemplate>
				<!-- DataTemplate x:DataType ： ItemSourceに指定したコレクションの要素にアクセスする時に用いるテンプレート引数型で指定したクラス名 -->
				<DataTemplate x:DataType="local:Person">
					<!-- TextBlock Text：{x:Bind変数 参照するメンバ名} -->
					<TextBlock Text="{x:Bind Name}" />
				</DataTemplate>
			</GridView.ItemTemplate>

			<GridView.ItemsPanel>
				<ItemsPanelTemplate>
					<ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Center" />
				</ItemsPanelTemplate>
			</GridView.ItemsPanel>
		</GridView>
	</Grid>
</Page>
